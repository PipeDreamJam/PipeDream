using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tunnel_Driver : MonoBehaviour {

    public GameObject tunnel;

	// Use this for initialization
	void Start () {
		//tunnel = Instantiate(tunnel, new Vector3(0, 0, 0), transform.rotation);
  //      tunnel.GetComponent<PieceManager>().ring_limit = 25;
  //      tunnel.GetComponent<PieceManager>().ring_count = 0;
    }
	
	// Update is called once per frame
	void Update () {

        //tunnel.GetComponent<PieceManager>().AddRing();
        //tunnel.GetComponent<PieceManager>().MoveRings();
    }
}
